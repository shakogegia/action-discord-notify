name: 'Discord Notification'
description: 'Greet someone and record the time'
inputs:
  status:
    required: false
  title:
    required: false
  mention:
    required: false
  mention_if:
    required: false
  url:
    required: true
runs:
  using: 'node12'
  main: 'src/index.js'